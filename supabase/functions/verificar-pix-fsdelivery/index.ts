import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.112.2";
const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(d:unknown,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{...CORS,"Content-Type":"application/json"}});
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:CORS});if(req.method!=="POST")return json({erro:"Método não permitido"},405);try{const url=Deno.env.get("SUPABASE_URL")||"";const key=Deno.env.get("SUPABASE_ANON_KEY")||"";const client=createClient(url,key,{global:{headers:{Authorization:req.headers.get("Authorization")||""}}});const {data:u,error:ue}=await client.auth.getUser();if(ue||!u.user)return json({erro:"Não autenticado"},401);const b=await req.json().catch(()=>({}));const id=String(b?.id||"");if(!id)return json({erro:"id obrigatório"},400);const {data,error}=await client.from("cobrancas_pix").select("id,txid,status,pago_em,processada_em,assinatura_id,vence_em").eq("id",id).eq("usuario_id",u.user.id).maybeSingle();if(error)throw error;return json({sucesso:true,cobranca:data})}catch(e){return json({erro:e instanceof Error?e.message:"Erro interno"},500)}});
