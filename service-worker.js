const CACHE='fsdelivery-operational-v5';
const CORE=['/css/style.css','/css/app-polish.css','/css/operational.css','/css/orders.css','/js/customer-order-access.js','/js/pedido-status.js','/js/app-orders-operational.js','/js/cliente.js','/icons/icon-192x192.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(CORE)).catch(()=>{}));self.skipWaiting()});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))));self.clients.claim()});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy)).catch(()=>{});return response}).catch(()=>caches.match(event.request)))});
self.addEventListener('push',event=>{let payload={};try{payload=event.data?.json()||{}}catch{payload={title:'FS Delivery',body:event.data?.text()||'Nova atualização operacional.'}}const title=payload.title||'FS Delivery';const options={body:payload.body||payload.message||'Nova atualização operacional.',icon:'/icons/icon-192x192.png',badge:'/icons/icon-192x192.png',tag:payload.tag||`fsdelivery-${Date.now()}`,renotify:true,vibrate:[180,80,180],data:{url:payload.url||'/'}};event.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('message',event=>{if(event.data?.type!=='SHOW_OPERATIONAL_NOTIFICATION')return;const p=event.data.payload||{};event.waitUntil(self.registration.showNotification(p.title||'FS Delivery',{body:p.body||'',icon:'/icons/icon-192x192.png',badge:'/icons/icon-192x192.png',tag:p.tag||`fsdelivery-${Date.now()}`,renotify:true,vibrate:[180,80,180],data:{url:p.url||'/'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=new URL(event.notification.data?.url||'/',self.location.origin).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{const existing=list.find(client=>client.url===url||client.url.startsWith(url));if(existing)return existing.focus();return clients.openWindow(url)}))});
