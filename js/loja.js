const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v)||0);const db=window.supabaseClient;const params=new URLSearchParams(location.search);const slug=params.get('loja');const tableToken=params.get('mesa');let settings,table=null,products=[],cart=[],current=null,qty=1;
async function init(){if(!slug)return showError('Loja não informada');const {data:est,error}=await db.from('estabelecimentos').select('*').eq('slug',slug).single();if(error||!est)return showError('Loja não encontrada');settings=est;if(tableToken){const {data:mesa}=await db.from('mesas').select('id,nome,identificacao,ativo').eq('estabelecimento_id',est.id).eq('token_publico',tableToken).eq('ativo',true).maybeSingle();if(!mesa)return showError('Mesa inválida ou indisponível');table=mesa}const {data}=await db.from('produtos').select('*,categorias(nome)').eq('estabelecimento_id',est.id).eq('ativo',true).order('created_at');products=(data||[]).map(p=>({id:p.id,name:p.nome,category:p.categorias?.nome||'Cardápio',price:Number(p.preco),description:p.descricao||'',image:p.imagem_url}));document.title=`${settings.nome} — Cardápio`;document.getElementById('public-store-name').textContent=settings.nome;document.getElementById('public-store-meta').textContent=`${settings.descricao||settings.categoria||'Delivery'} • ${settings.tempo_entrega_min}–${settings.tempo_entrega_max} min`;document.getElementById('public-store-status').textContent=settings.aberto?'Aberto agora':'Fechado agora';configureContext();renderMenu();renderCart()}
function showError(message){document.getElementById('menu-content').innerHTML=`<div class="empty-state"><h3>${message}</h3></div>`}
function configureContext(){const select=document.getElementById('delivery-type'),label=document.getElementById('order-context-label'),tableInfo=document.getElementById('table-context');if(table){const name=table.nome||`Mesa ${table.identificacao}`;tableInfo.hidden=false;tableInfo.textContent=`Pedido local • ${name}`;label.textContent=name;select.innerHTML='<option value="mesa">Pedido nesta mesa</option>';document.getElementById('address-field').style.display='none';document.querySelector('a[href="cliente.html"]').style.display='none'}else{label.textContent='Pedido on-line';select.innerHTML='<option value="delivery">Entrega</option><option value="pickup">Retirada no balcão</option><option value="local">Comer no local</option>';document.getElementById('address-field').style.display='grid'}updateCheckoutTotal()}
function openModal(id){document.getElementById(id).classList.add('open');document.body.style.overflow='hidden'}function closeModals(){document.querySelectorAll('.modal').forEach(m=>m.classList.remove('open'));document.body.style.overflow=''}document.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeModals);document.querySelectorAll('.modal').forEach(m=>m.onclick=e=>{if(e.target===m)closeModals()});
function renderMenu(){const cats=[...new Set(products.map(p=>p.category))];document.getElementById('category-tabs').innerHTML=cats.map((c,i)=>`<button class="${i?'':'active'}" data-cat="${c}">${c}</button>`).join('');document.getElementById('menu-content').innerHTML=cats.length?cats.map(c=>`<section class="menu-section" id="cat-${c}"><h2>${c}</h2><div class="menu-products">${products.filter(p=>p.category===c).map(p=>`<article class="menu-product" data-product="${p.id}"><div><h3>${p.name}</h3><p>${p.description}</p><strong>${money(p.price)}</strong></div>${p.image?`<img alt="${p.name}" src="${p.image}">`:''}</article>`).join('')}</div></section>`).join(''):'<div class="empty-state"><h3>Cardápio indisponível</h3></div>';document.querySelectorAll('[data-product]').forEach(el=>el.onclick=()=>showProduct(el.dataset.product));document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{document.getElementById(`cat-${b.dataset.cat}`).scrollIntoView({behavior:'smooth'});document.querySelectorAll('[data-cat]').forEach(x=>x.classList.toggle('active',x===b))})}
function showProduct(id){if(!settings.aberto)return alert('A loja está fechada no momento.');current=products.find(p=>p.id===id);qty=1;document.getElementById('qty-value').textContent=qty;document.getElementById('store-product-title').textContent=current.name;document.getElementById('store-product-description').textContent=current.description;document.getElementById('item-note').value='';document.getElementById('store-options').innerHTML='';updateAddButton();openModal('product-store-modal')}
function updateAddButton(){document.getElementById('add-cart-btn').textContent=`Adicionar • ${money((current?.price||0)*qty)}`}document.getElementById('qty-minus').onclick=()=>{qty=Math.max(1,qty-1);document.getElementById('qty-value').textContent=qty;updateAddButton()};document.getElementById('qty-plus').onclick=()=>{qty++;document.getElementById('qty-value').textContent=qty;updateAddButton()};document.getElementById('add-cart-btn').onclick=()=>{cart.push({cartId:crypto.randomUUID(),productId:current.id,name:current.name,price:current.price,qty,note:document.getElementById('item-note').value});closeModals();renderCart()}}
function subtotal(){return cart.reduce((s,i)=>s+i.price*i.qty,0)}function orderType(){return table?'mesa':document.getElementById('delivery-type').value}function deliveryFee(){return orderType()==='delivery'?Number(settings?.taxa_entrega||0):0}function total(){return subtotal()+deliveryFee()}
function renderCart(){document.getElementById('cart-items').innerHTML=cart.length?cart.map(i=>`<div class="cart-item"><div class="cart-item-line"><b>${i.qty}x ${i.name}</b><b>${money(i.price*i.qty)}</b></div>${i.note?`<small>${i.note}</small>`:''}<button class="link-button" data-remove="${i.cartId}">Remover</button></div>`).join(''):'<div class="cart-empty">Seu carrinho está vazio.</div>';document.getElementById('cart-total').textContent=money(subtotal());document.getElementById('mobile-total').textContent=money(subtotal());document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{cart=cart.filter(i=>i.cartId!==b.dataset.remove);renderCart()});updateCheckoutTotal()}
function updateCheckoutTotal(){if(!settings)return;const type=orderType();document.getElementById('address-field').style.display=type==='delivery'?'grid':'none';document.getElementById('checkout-total-label').textContent=type==='delivery'?'Total com entrega':'Total';document.getElementById('checkout-total').textContent=money(total())}
function checkout(){if(!settings.aberto)return alert('A loja está fechada.');if(!cart.length)return alert('Adicione ao menos um produto.');if(!table&&subtotal()<Number(settings.pedido_minimo))return alert(`O pedido mínimo é ${money(settings.pedido_minimo)}.`);updateCheckoutTotal();openModal('checkout-modal')}document.getElementById('checkout-btn').onclick=checkout;document.getElementById('mobile-cart').onclick=checkout;document.getElementById('delivery-type').onchange=updateCheckoutTotal;
document.getElementById('checkout-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),type=orderType();if(type==='delivery'&&!String(f.get('address')||'').trim())return alert('Informe o endereço completo.');const payload={slug,nome:f.get('name'),telefone:f.get('phone'),tipo:type,endereco:type==='delivery'?f.get('address'):null,pagamento:f.get('payment'),observacoes:f.get('notes'),mesa_token:tableToken||null,itens:cart.map(i=>({produto_id:i.productId,quantidade:i.qty,observacoes:i.note}))};const {data,error}=await db.rpc('criar_pedido_publico',{payload});if(error)return alert(error.message);closeModals();const context=table?` para ${table.nome||`Mesa ${table.identificacao}`}`:'';document.getElementById('success-message').textContent=`Pedido #${data} recebido${context}. Total: ${money(total())}.`;cart=[];renderCart();openModal('success-modal')};init();