(()=>{
 const Status=window.FSOrderStatus,esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
 let routeOrders=[];
 function card(order,index,action){const phone=normalizePhone(order.cliente_telefone),address=addressOf(order),button=action?`<button class="btn btn-primary" data-delivery="${order.id}" data-status="${action.status}">${action.label}</button>`:'';return`<article class="order-card"><span class="delivery-route-index">${index+1}</span><div class="order-main"><b>#${esc(order.codigo||order.id)} • ${esc(order.cliente_nome||'Cliente')}</b><small class="delivery-address">${esc(address)}</small><div class="delivery-meta"><span>${esc(order.cliente_telefone||'Sem telefone')}</span><span>${money(order.total)}</span><span>${esc(Status.label(order.status,'entrega'))}</span></div><div class="delivery-actions">${phone?`<a class="btn btn-secondary" href="tel:+55${phone}">Ligar</a>`:''}${!['preparo','finalizado','entregue'].includes(order.status)?`<button class="btn btn-secondary" data-map="${order.id}">Navegar</button>`:''}${button}</div></div></article>`}
 function group(title,subtitle,list,actionFor){return`<section class="delivery-group"><h3>${title}</h3><small>${subtitle}</small>${list.length?list.map((order,index)=>card(order,index,actionFor(order))).join(''):'<div class="empty-state">Nenhum pedido nesta etapa.</div>'}</section>`}
 renderOrders=function(data){data.forEach(order=>order.status=Status.normalize(order.status));const preparing=data.filter(order=>order.status==='preparo'),ready=data.filter(order=>order.status==='pronto'),active=data.filter(order=>order.status==='saiu_entrega'),done=data.filter(order=>order.status==='finalizado');routeOrders=optimize([...active,...ready],currentPosition);currentOrders=data;pendingCount.textContent=String(ready.length);activeCount.textContent=String(active.length);routeButton.disabled=!routeOrders.length;orderList.innerHTML=group('Em preparo','Antecipe as próximas entregas. Ainda não é possível iniciar a rota.',preparing,()=>null)+group('Prontos para entrega','Pedidos liberados pela cozinha.',ready,()=>({status:'saiu_entrega',label:'Iniciar entrega'}))+group('Em rota','Entregas em andamento.',active,()=>({status:'entregue',label:'Confirmar entrega'}))+group('Concluídas hoje','Histórico compacto das entregas finalizadas.',done,()=>null);orderList.querySelectorAll('[data-map]').forEach(button=>button.onclick=()=>openSingleRoute(data.find(order=>String(order.id)===button.dataset.map)));orderList.querySelectorAll('[data-delivery]').forEach(button=>button.onclick=()=>updateDelivery(button))};
 openFullRoute=function(){if(!routeOrders.length)return alert('Nenhuma entrega pronta ou em rota.');const points=routeOrders.map(mapsDestination).filter(Boolean),destination=points.pop(),origin=currentPosition?`${currentPosition.lat},${currentPosition.lng}`:'',params=new URLSearchParams({api:'1',destination,travelmode:'driving'});if(origin)params.set('origin',origin);if(points.length)params.set('waypoints',points.slice(0,8).join('|'));window.open(`https://www.google.com/maps/dir/?${params}`,'_blank','noopener')};routeButton.onclick=openFullRoute;const head=document.querySelector('.panel-head p');if(head)head.textContent='Acompanhe pedidos em preparo, prontos, em rota e concluídos.';loadDeliveries();
})();
